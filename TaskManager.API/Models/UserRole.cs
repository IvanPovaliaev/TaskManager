namespace TaskManager.API.Models
{
    public enum UserRole
    {
        Admin,
        Editor,
        User
    }
    public enum ProjectStatus
    {
        InProgress,
        Suspended,
        Completed
    }
}
